"""
Deprecated: this duplicate test module has moved to tests/test_entities.py
with corrected imports. Kept here as an inert placeholder (rather than
deleted, since the file could not be removed in this environment) so
pytest doesn't try to collect a second, import-broken copy.
"""
